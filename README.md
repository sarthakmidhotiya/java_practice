# 📌 Data Types in Java

In Java, data types define the type of value a variable can store.  
They are mainly divided into **two categories**:

---

## 1️⃣ Primitive Data Types

Primitive data types are basic built-in data types provided by Java.  
They store simple values and are not objects.

### 🔹 A) Non-Numeric Types

These types do not store numbers.

- **boolean** → Stores only `true` or `false`.
- **char** → Stores a single character (e.g., 'A', 'b', '1').

---

### 🔹 B) Numeric Types

These types store numbers.

#### ➤ Integer Types (Whole Numbers)

Used to store whole numbers (without decimal point):

- **byte** → 1 byte (range: -128 to 127)
- **short** → 2 bytes (range: -32,768 to 32,767)
- **int** → 4 bytes (most commonly used)
- **long** → 8 bytes (used for large integer values)

#### ➤ Floating Point Types (Decimal Numbers)

Used to store numbers with decimal values:

- **float** → 4 bytes (single precision)
- **double** → 8 bytes (double precision, commonly used)

---

## 2️⃣ Non-Primitive Data Types

Non-primitive data types are reference types.  
They store memory addresses instead of actual values.

Examples:

- **String** → Stores a sequence of characters (e.g., "Hello")
- **Array** → Stores multiple values of the same type
- **Class**
- **Object**
- **Interface**
- etc.

---

## 🧠 Summary

- Primitive types store actual values.
- Non-Primitive types store references (memory addresses).
- Java has **8 primitive data types**:

```
byte, short, int, long,
float, double,
char, boolean
```

---
